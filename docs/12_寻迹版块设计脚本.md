# 「寻迹」版块设计脚本 V2

> 核心修订：① 配色统一至 Echo 暖奶油调；② 收支卡改为紧凑Tab式；③ 事件流改为「流水漂字」动效

---

## 一、配色系统（严格复用现有变量）

> [!IMPORTANT]
> 不引入任何新底色。所有卡片用白底 `#FFFFFF`，页面底用 `var(--bg-secondary)` (#F7F5F1)，
> 各版块的**彩色只用于头部accent条、图标、数字高亮**，保持整体暖奶油统一感。

| 版块 | 使用的现有变量 | 用途 |
|------|-------------|------|
| 收支 | `--grad-warm` (#F5A623→#F4836A) | 标题 accent 条、金额高亮色 |
| 情绪 | `--grad-purple` (#9D8FE0→#7B6EC6) | 标题 accent 条、情绪 chip 底色 |
| 事件流水 | `--grad-sky` (#6BB8E8→#5A76D8) | 整个流水区背景渐变 |
| 地点 | `--accent-light` + `--accent` | 泡泡底色 + 文字 |
| 灵感 | `--grad-coral` (#F4836A→#E05FA8) | 灵感 chip 文字色 |

参考图1的配色逻辑：**白卡 + 彩色小 accent + 大量留白**，与 Echo 已有设计完全一致。

---

## 二、页面整体布局

```
┌─────────────────────────────┐  
│  App Header (Echo · 退出)   │  ← 复用现有 .app-header
├─────────────────────────────┤
│  Hero：「寻迹」标题 +        │  ← 高度约 110px
│         时间段选择器         │
├─────────────────────────────┤
│  💰 收支洞察卡（Tab式紧凑）  │  ← 高度约 200px（含三Tab）
├─────────────────────────────┤
│  😊 情绪图谱卡               │  ← 高度约 220px
├─────────────────────────────┤
│  🌊 事件流水区（动效）        │  ← 高度固定 180px
├─────────────────────────────┤
│  📍 地点足迹                 │  ← 高度约 130px
├─────────────────────────────┤
│  🏷️ 灵感云                  │  ← 高度约 120px
├─────────────────────────────┤
│  Bottom Nav                 │
└─────────────────────────────┘
```

---

## 三、Hero 区

### 视觉规范
- **背景**：`var(--bg-secondary)` (#F7F5F1)，无特殊纹理，保持与整个 App 一致的暖奶油色
- **左侧**：
  - 小标签文字：`寻迹 · TRACE`，字号 11px，大写，`var(--text-muted)`，letter-spacing 宽
  - 大标题：`发现你的足迹`，28px，font-weight 800，`var(--text-primary)`
  - 副标文字：`本月 · 4月`（动态），14px，`var(--text-secondary)`
- **右侧**：装饰 —— 一个 40×40 的橙色渐变圆，内含 🔍，`background: var(--grad-warm)`，`border-radius: 50%`，轻微浮动动画

### 时间段选择器
- 四个胶囊按钮：`本周` `本月` `近三月` `全部`
- 复用思路：类似 `index.html` 已有的 filter tab 样式
- **激活态**：`background: var(--accent); color: #fff; border-radius: 20px`
- **非激活态**：`background: #fff; color: var(--text-secondary); border: 1px solid var(--border)`
- 间距：`gap: 6px`，整行 `padding: 12px 16px`

---

## 四、① 收支洞察卡 💰（核心修改：紧凑 Tab 式）

### 设计理念
> 把三层信息（概览/分类/明细）折叠进一张卡，**默认展示最有价值的概览**，用户主动切换才展开细节。卡片高度控制在 200px 以内。

### 卡片外观
- 白底，顶部 3px accent 条：`background: var(--grad-warm)`
- 卡片标题行：`💰 收支洞察` + 右侧三个 Tab 切换按钮（`概览 / 分类 / 明细`）
  - Tab 本身极小：font-size 12px，padding 3px 8px，圆角，无边框
  - 激活 Tab：`background: var(--accent-light); color: var(--accent); font-weight: 600`

### Tab 内容区（高度约 130px，内容切换用 JS display 控制）

**Tab 1 — 概览（默认展示）**
```
┌─────────────────────────────────────────────┐
│  本期支出              最大单笔              │
│  ¥ 2,340.50           ¥ 680 · 餐饮          │
│  [金色渐变大字 26px]  [小字 13px, 右侧]      │
│                                             │
│  ████░░░░░░ 餐饮 45%   ████░░░░░░ 交通 22%   │ ← 仅显示前2类
│                                             │
└─────────────────────────────────────────────┘
```
- 概览只显示 2 条 mini bar（各分类占总额百分比）
- bar 高度：6px，圆角，颜色：`var(--accent)` 和 `var(--accent-2)`

**Tab 2 — 分类（横向滚动）**
```
┌──────────────────────────────────────────────┐
│  [🍔餐饮 ¥1,050] [🚗交通 ¥510] [🛒购物 ¥320]... │
│                  ← 水平滚动 →                │
│  ████████░░  餐饮 45%                        │
│  █████░░░░░  交通 22%                        │
│  ████░░░░░░  购物 14%  ...（最多5条）         │
└──────────────────────────────────────────────┘
```
- 顶部：横向可滚动的分类选择器 chip（点击高亮该类，bar 图聚焦）
- 下方：垂直 bar 列表

**Tab 3 — 明细**
```
  04/20  超市购物       -¥128.00
  04/19  咖啡馆         -¥38.50
  04/18  地铁           -¥12.00
           ...（最多8条，可点击跳转）
```
- 每行：日期 chip（小圆角，`var(--bg-muted)`）+ 描述 + 金额（右对齐，`var(--accent-dark)`）
- 整个明细区 `overflow-y: auto; max-height: 130px`

### countUp 动画
- 仅对"本期支出"大数字做 countUp（0.6s ease-out cubic）
- 每次切换时间范围时重新触发

---

## 五、② 情绪图谱卡 😊

### 卡片外观
- 白底，顶部 3px accent 条：`background: var(--grad-purple)`
- 标题：`😊 心情谱`，副标：`情绪的涟漪`（11px，`var(--text-muted)`）

### 内容结构

**上半：情绪折线 Sparkline（高度约 70px）**
- 用 inline SVG 绘制，不引入图表库
- X 轴：近 7 天的日期（或本月按周聚合）
- Y 轴：`emotion_overall_score`（0~10）
- 线条颜色：`#9D8FE0`，线宽 2px
- 线下填充：`rgba(157,143,224,0.15)` 渐变至透明
- 数据点：直径 6px 圆点，hover 显示浮层（日期 + 分值）
- 无数据日期的点：空心圆，`var(--border)`

**下半：情绪标签墙**
- 调用 `/history/emotions?page_size=100`，按 `emotion_label` 聚合 + 计数
- 每个 chip：`border-radius: 20px; padding: 4px 12px; font-size: 12px`
- **词频驱动字号**：1次→12px，每+1次→+1.5px，最大 20px
- **颜色 mapping（前端写死 Map）**：

```javascript
const EMOTION_COLORS = {
  '开心': { bg: '#FEF3E2', color: '#F5A623' },   // 暖金（复用 accent-light）
  '快乐': { bg: '#FEF3E2', color: '#F5A623' },
  '平静': { bg: '#E8F7EE', color: '#4CAF7D' },   // 薄荷绿（复用 success）
  '焦虑': { bg: '#FDECEA', color: '#E05555' },   // 珊瑚红（复用 danger）
  '悲伤': { bg: '#EEF4FD', color: '#5A76D8' },   // 天空蓝
  '愤怒': { bg: '#FDEAF3', color: '#E05FA8' },   // 玫红
  'default': { bg: '#EDEBFC', color: '#9D8FE0' } // 柔紫（复用 accent-2）
};
```

- 排列：`flex-wrap: wrap; gap: 6px`，chip 间距均匀

---

## 六、③ 事件流水区 🌊（核心创意版块）

### 设计概念
> 整个版块是一块「水面」，事件文字如同漂在水里的树叶，缓缓向上漂浮，然后在顶部消失，循环往复。给人一种「时光流逝」的诗意感。

### 容器规范
```css
.xj-stream {
  height: 180px;
  border-radius: var(--radius-lg);
  background: linear-gradient(180deg, #E8F4FD 0%, #C8E8F8 40%, #A8D8F0 100%);
  /* 模拟水面：顶浅底深的蓝绿渐变，用现有 --grad-sky 衍生 */
  position: relative;
  overflow: hidden;
  margin: 0 16px;
}
```

### 水纹效果（纯 CSS）
```css
/* 用两个伪元素做波浪层叠 */
.xj-stream::before,
.xj-stream::after {
  content: '';
  position: absolute;
  width: 200%;
  height: 40px;
  top: 20px;
  left: -50%;
  background: rgba(255,255,255,0.18);
  border-radius: 50%;
  animation: xj-wave 4s ease-in-out infinite;
}
.xj-stream::after {
  top: 35px;
  animation-delay: -2s;
  background: rgba(255,255,255,0.10);
}
@keyframes xj-wave {
  0%, 100% { transform: translateX(0) scaleY(1); }
  50% { transform: translateX(8%) scaleY(0.8); }
}
```

### 漂浮文字规范
```css
.xj-float-word {
  position: absolute;
  white-space: nowrap;
  font-size: 13px;
  font-weight: 500;
  color: rgba(30, 80, 130, 0.75);   /* 深蓝透明，像水里看文字 */
  pointer-events: none;
  animation: xj-float-up var(--dur, 8s) linear var(--delay, 0s) infinite;
  text-shadow: 0 1px 3px rgba(255,255,255,0.5);
}
@keyframes xj-float-up {
  0%   { transform: translateY(180px) translateX(0px);    opacity: 0; }
  10%  { opacity: 0.8; }
  85%  { opacity: 0.7; }
  100% { transform: translateY(-30px) translateX(var(--drift, 5px)); opacity: 0; }
}
```

### JS 渲染逻辑
```javascript
function renderEventStream(items) {
  const container = document.getElementById('xjStream');
  // 清除旧内容（保留伪元素水波）
  container.querySelectorAll('.xj-float-word').forEach(el => el.remove());

  if (!items || items.length === 0) {
    container.innerHTML += '<div class="xj-stream-empty">还没有事件记录</div>';
    return;
  }

  // 最多取 15 条事件，截断过长的文字
  const words = items.slice(0, 15).map(e => {
    const text = e.content.length > 18 ? e.content.slice(0, 18) + '…' : e.content;
    return text;
  });

  words.forEach((text, i) => {
    const el = document.createElement('div');
    el.className = 'xj-float-word';
    el.textContent = text;

    // 随机化：起始X位置、漂浮速度、延迟、横向偏移
    const x = 10 + Math.random() * 70;   // 10% ~ 80% 宽度
    const dur = 7 + Math.random() * 6;   // 7s ~ 13s 一轮
    const delay = -(Math.random() * dur); // 负延迟让初始分布不集中
    const drift = (Math.random() - 0.5) * 30; // -15px ~ +15px 横向漂移

    el.style.cssText = `
      left: ${x}%;
      --dur: ${dur}s;
      --delay: ${delay}s;
      --drift: ${drift}px;
      animation-delay: ${delay}s;
      animation-duration: ${dur}s;
    `;
    container.appendChild(el);
  });
}
```

### 版块标题
- 位于卡片外部（.section-row 样式）：`🌊 事件流` + 副标文字「记忆漂浮其上」
- 标题文字颜色：`var(--grad-sky)` 渐变裁剪

### 空状态
- 在容器内居中：半透明白色文字「还没有事件记录，去今天聊聊吧」

---

## 七、④ 地点足迹 📍

### 外观
- 白底卡片，顶部标题：`📍 足迹` + 副标：`走过的地方`
- 地点以泡泡 chip 排列：`background: var(--accent-light); color: var(--accent-dark)`
- 去重 + 显示出现次数：`📍 星巴克 ×3`
- 最多 10 个，超出折叠

---

## 八、⑤ 灵感云 🏷️

### 外观
- 白底卡片，标题：`💡 灵感`
- 灵感 chip：词频大的字号大（12~20px），颜色来自 EMOTION_COLORS 类似的映射，或直接用 `var(--accent-2)` 系列
- 点击跳转：`href="index.html?tag=xxx"`（与"回忆"页的 tag 筛选联动）

---

## 九、CSS 新增类命名规范

```css
/* Hero 区 */
.xj-hero          /* hero 容器 */
.xj-title         /* 大标题 */
.xj-range-tabs    /* 时间段选择器组 */
.xj-range-tab     /* 单个按钮 */
.xj-range-tab.active

/* 通用卡片装饰 */
.xj-card          /* 在现有 .card 基础上加顶部 accent 条 */
.xj-accent-warm   /* --grad-warm 的 accent 条 */
.xj-accent-purple /* --grad-purple 的 accent 条 */
.xj-accent-sky    /* --grad-sky 的 accent 条 */

/* 收支 Tab 式卡片 */
.xj-expense-tabs      /* Tab 按钮组 */
.xj-expense-tab       /* 单个 Tab */
.xj-expense-tab.active
.xj-expense-pane      /* 每个 Tab 的内容区，display:none/block 切换 */
.xj-expense-hero-num  /* 大金额数字 */
.xj-mini-bar          /* 概览 mini bar */
.xj-cat-bar           /* 分类 bar */
.xj-ledger-row        /* 明细列表行 */

/* 情绪 */
.xj-spark-svg         /* sparkline SVG 容器 */
.xj-emotion-chip      /* 情绪灵感 chip */

/* 流水区 */
.xj-stream            /* 流水容器 */
.xj-float-word        /* 漂浮文字 */
.xj-stream-empty      /* 空状态 */

/* 地点 */
.xj-place-chip        /* 地点泡泡 */

/* 灵感 */
.xj-tag-item          /* 灵感 chip */
```

---

## 十、JS 架构（精简版）

```javascript
// ── 状态 ──────────────────────────────────────────
const XJ = {
  range: 'month',
  cache: {}
};

// ── 入口 ──────────────────────────────────────────
async function initTrace() {
  if (!requireAuth()) return;
  updateRangeLabel();
  await loadAll();
}

// ── 时间范围 ──────────────────────────────────────
function setRange(r) {
  XJ.range = r;
  document.querySelectorAll('.xj-range-tab').forEach(t => {
    t.classList.toggle('active', t.dataset.range === r);
  });
  updateRangeLabel();
  loadAll();
}

function getDateRange() {
  const now = new Date();
  const end = now.toISOString().slice(0, 10);
  let start;
  const r = XJ.range;
  if (r === 'week') {
    const d = new Date(now); d.setDate(d.getDate() - 6);
    start = d.toISOString().slice(0, 10);
  } else if (r === 'month') {
    start = `${now.getFullYear()}-${String(now.getMonth()+1).padStart(2,'0')}-01`;
  } else if (r === 'quarter') {
    const d = new Date(now); d.setMonth(d.getMonth() - 3);
    start = d.toISOString().slice(0, 10);
  } else {
    start = '2000-01-01';
  }
  return { start, end };
}

// ── 并行加载 ──────────────────────────────────────
async function loadAll() {
  const { start, end } = getDateRange();
  const qs = `start_date=${start}&end_date=${end}&page_size=100`;
  const now = new Date();
  const calQs = `year=${now.getFullYear()}&month=${now.getMonth()+1}`;

  const [expenses, emotions, events, locations, inspirations, calendar] =
    await Promise.allSettled([
      apiFetch(`/history/expenses?${qs}`),
      apiFetch(`/history/emotions?${qs}`),
      apiFetch(`/history/events?${qs}`),
      apiFetch(`/history/locations?${qs}`),
      apiFetch(`/history/inspirations?${qs}`),
      apiFetch(`/history/calendar?${calQs}`)
    ]);

  renderExpenses(ok(expenses));
  renderEmotions(ok(emotions), ok(calendar));
  renderEventStream(ok(events)?.records);
  renderLocations(ok(locations));
  renderInspirations(ok(inspirations));
}

const ok = res => res.status === 'fulfilled' ? res.value : null;

// ── countUp ──────────────────────────────────────
function countUp(el, target, prefix = '¥') {
  const t0 = performance.now();
  const dur = 700;
  const step = now => {
    const p = Math.min((now - t0) / dur, 1);
    const ease = 1 - (1 - p) ** 3;
    el.textContent = prefix + (target * ease).toFixed(2);
    if (p < 1) requestAnimationFrame(step);
  };
  requestAnimationFrame(step);
}
```

---

## 十一、导航改动（4个文件）

```html
<!-- today.html / index.html / profile.html / trace.html 中的底部导航 -->
<a class="app-nav-item" href="trace.html">
  <span class="app-nav-icon">🔍</span>
  <span class="app-nav-label">寻迹</span>
</a>
```

文件重命名：`hope.html` → `trace.html`

---

## 十二、实现优先级

| 优先级 | 内容 | 接口 | 难度 |
|--------|------|------|------|
| **P0** | Hero + 时间段选择器 + 并行加载框架 | — | 低 |
| **P0** | 收支卡（Tab三屏：概览/分类/明细） | `/history/expenses` | 中 |
| **P0** | 情绪标签墙 | `/history/emotions` | 低 |
| **P0** | 事件流水动效（漂浮文字） | `/history/events` | 中 |
| **P1** | 情绪折线 Sparkline SVG | `/history/calendar` | 中高 |
| **P1** | 地点泡泡 + 灵感云 | `/history/locations` `/history/inspirations` | 低 |
| **P2** | countUp 动画、分类 bar grow 动画 | — | 低 |
| **P2** | 灵感点击 → `index.html?tag=xxx` 联动 | — | 低 |
